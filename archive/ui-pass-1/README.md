# UI Pass 1 Archive

This directory preserves the original visual prototype before the editor was simplified around working interactions. It is reference-only and is not included in the active build.
