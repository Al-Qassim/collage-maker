export interface CanvasSettings {
  width: number;
  height: number;
  spacing: number;
  radius: number;
}

export const DEFAULT_CANVAS_SETTINGS: CanvasSettings = {
  width: 1080,
  height: 1350,
  spacing: 0,
  radius: 0,
};
